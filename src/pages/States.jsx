import { Link } from "react-router-dom"; import {useMemo,useState} from "react"; import states from "../data/states"; import {photoFor} from "../data/destinations";
export default function States(){const [term,setTerm]=useState("");const items=useMemo(()=>states.filter(s=>s.name.toLowerCase().includes(term.toLowerCase())),[term]);return <main className="page"><div className="page-heading"><p className="intro-label">EXPLORE INDIA</p><h1>States & Union Territories</h1><p>Choose a state to see its destinations and travel information.</p><input className="filter-input" value={term} onChange={e=>setTerm(e.target.value)} placeholder="Search a state or UT…"/></div><div className="state-grid">{items.map(s=><article className="state-card" key={s.id}><img src={photoFor(`${s.name} tourism`,s.id)} alt={s.name}/><div className="state-content"><p>{s.capital}</p><h2>{s.name}</h2><span>{s.places.length} destinations · {s.language}</span><Link to={`/states/${s.id}`} className="details-btn">Explore state →</Link></div></article>)}</div></main>}
